import React, { useRef } from 'react';
import './pages.scss';

const Login: React.FC = () => {
    useRef<HTMLDivElement>(null);
    return (
        <div>
            Login
        </div>
    );
};

export default Login;
